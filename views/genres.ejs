<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>

<body>
  <main>
    <%- include('./partials/header.ejs') %>

    <h1>Genres page</h1>
    <% genres.map(genre => { %>
    <h2><%= genre.name  %></h2>
    <% }) %>
  </main>
</body>

</html>